server:
  port: 7071

spring:
  application:
    name: test-client
eureka:
  client:
    serviceUrl:
         defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true # 启用ip配置 这样在注册中心列表中看见的是以ip+端口呈现的

